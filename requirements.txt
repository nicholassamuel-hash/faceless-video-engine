# Core
pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
python-dotenv>=1.0

# TTS
edge-tts>=6.1.10

# Clipping (YouTube -> Shorts)
yt-dlp>=2025.1.1

# Networking / retries
httpx>=0.27
tenacity>=8.2

# Google / YouTube upload
google-api-python-client>=2.120
google-auth>=2.29
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# Optional LLM provider (script generation). The default LLMProvider is a
# local template stub; install + configure a real provider to swap it in.
# anthropic>=0.40
# openai>=1.30

# Dev / tests
pytest>=8.0

# NOTE: ffmpeg + ffprobe are NOT pip packages. Install the ffmpeg binaries
# separately and make sure they are on PATH. On Windows:
#   winget install Gyan.FFmpeg
# or download from https://www.gyan.dev/ffmpeg/builds/ and add the bin/ dir
# to your PATH.
